apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: "http://{{prometheus_host}}:9090"
    isDefault: true
    basicAuth: true
    basicAuthUser: admin
    secureJsonData:
      basicAuthPassword: {{prometheus_admin_password}}
